line-length = 100

[lint]
select = ["E", "F", "I", "UP", "B", "SIM", "C90"]

[lint.mccabe]
max-complexity = 20

[lint.per-file-ignores]
# typer's API puts function calls in parameter defaults by design
"packages/ngen-weave-cli/src/ngen_weave_cli/main.py" = ["B008"]
